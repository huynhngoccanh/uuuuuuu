Hi <%= @vendor.first_name %>!

<% if @email_content.hello_sub_text.blank? %>
    Recently you won one of our searches #<%= @search.id %> for "<%= @search.search %>".
<% else %>
    <%= prepare_email_content(@email_content.hello_sub_text, :auction => @auction, :vendor => @vendor) %>
<% end %>

The customer claimed that he choose your offer and you made that transaction.

Click te link below to deny or accept this claim:

<%= outcome_company_auction_url(@auction)  %>
