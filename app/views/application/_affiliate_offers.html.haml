- unless @combined_affiliate_offers.blank?
  .affiliate-offers
    - @combined_affiliate_offers.each_with_index do |offer, idx|
      - next if offer.advertiser.blank?
      .offer-box{:class=> ((idx%2).zero? ? 'last-in-line': '')}
        - if offer.is_a? CjOffer
          = render 'cj_offer', :id => idx, :offer=>offer
          = render 'cj_offer_details', :id => idx, :offer=>offer
        - if offer.is_a? AvantOffer
          = render 'avant_offer', :id => idx, :offer=>offer
          = render 'avant_offer_details', :id => idx, :offer=>offer
  - if @just_avant_fetched
    .affiliate-offers-message
      .offers-spinner
      We're matching instant offers for you. Please wait few seconds.
  .offers-disclaimer
    %ul
      %li You are eligible for cash-back on all purchases from the above merchants, not only your auctioned item
      %li It normally takes 24-48 hours after your purchase for cash back to register in your MuddleMe account
      %li Transactions completed from mobile phones and tablets will not register your cash back, please use your PC or MAC
- else
  - if @just_avant_fetched
    .affiliate-offers-message
      .offers-spinner
      We're matching instant offers for you. Please wait few seconds.
  - else        
    .affiliate-offers-message
      Sorry, no instant offers found


%section.bubble-box#cash-back
  %h3
    Cash back on
    %span all purchases
    from these
    merchants.
    %br
    %a{:href => new_user_registration_path} SIGN UP FREE