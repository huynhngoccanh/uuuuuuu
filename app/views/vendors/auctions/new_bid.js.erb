$('#new-bid-form').html("<%= escape_javascript render('new_bid_form') %>");
$('#bid_offer_id').multiselect({
  header: false,
  selectedList: 1,
  height: "auto",
  minWidth: "235",
  multiple: false,
  classes: "multiselect-button multiselect-dropdown",
  position: {
    my: 'left top',
    at: 'left top'
  }
});
initFormValidations('#new-bid-form form');