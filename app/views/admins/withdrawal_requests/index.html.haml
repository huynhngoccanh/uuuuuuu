- content_for :sub_nav do
  = render 'admins/sub_nav', :active_action=>'index', :active_controller=>'admins/withdrawal_requests'

- content_for :main_column do
  %h2 All withdrawal requests
  = render "wr_list"

= render 'layouts/application/one_column'
